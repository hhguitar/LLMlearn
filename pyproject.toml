[project]
name = "tesla-qa-system"
version = "0.1.0"
description = "Cross-year Tesla filing QA system with hybrid retrieval"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
